# Non-secret function parameters. Secrets live in .env.secrets instead,
# because Firebase turns every .env value into a plain environment variable
# and that collides with a Secret Manager binding of the same name.
GMAIL_OAUTH_CLIENT_ID=
GMAIL_PUBSUB_TOPIC=
